<%= form_for([:contact,@note], :html => {:id => "notes"}) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note ,:size => "4x6" ,:class =>"field1"%>
  </div>
   <% if @note.contact_id.nil? %>
   <%= f.hidden_field :contact_id, :value => params[:contact_id] %>
   <% end %>
 <br/>
  <div class="actions">
    <%= f.submit "Add note", :class => "button" %>
  </div>
<% end %>
