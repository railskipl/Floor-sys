
<div class="formbox">
<%= form_for(@defective_product) do |f| %>
  <% if @defective_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@defective_product.errors.count, "error") %> prohibited this defective_product from being saved:</h2>

      <ul>
      <% @defective_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <table width="60%" border="0" cellspacing="10" cellpadding="5">
    <tr>
      <td align="right" valign="middle">Product </td>
      <td align="center" valign="middle">:</td>
      <td> <%= f.select "product_id", options_from_collection_for_select(@product, "id", "product_name"),{ :include_blank => "-- Select---" }, :onchange => remote_function(:url => {:action => :find1 }, :with => "'product_id='+value" ) %></td>
    </tr>
    <tr>
      <td align="right" valign="middle">Notes </td>
      <td align="center" valign="middle">:</td>
      <td><%= f.text_area :notes, :rows => 3,:class =>"field1" %></td>
    </tr>
     <div id="uom">
     	  	
     </div>

  
     <div id="unit">
    	
     </div>
  
  
    <tr>        
     
        <%= f.hidden_field :company_id, :value =>"#{current_user.company_id}" %>               
    </tr>
 
    <tr> 
      <td align="right" valign="middle">&nbsp;</td>
      <td align="center" valign="middle">&nbsp;</td>
      <td><%= f.submit "submit", :class =>'button'%></td>
   </tr>
   
  </table>
  <% end %>

<%= link_to 'Back', defective_products_path %>
 </div>