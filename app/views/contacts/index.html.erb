<h1>Listing vats</h1>

<table>
  <tr>
    <th>Tax rate</th>
    
    <th>Company</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @contacts.each do |contact| %>
  <tr>
    <td><%= contact.contact_name %></td>
    
    <td><%= contact.contact_position %></td>
    <td><%= link_to 'Show', contact %></td>
    
    <td><%= link_to 'Destroy', {:controller=>"contacts", :action=>"destroy", :id=>"#{contact.id}"} , { :confirm => 'Are you sure?'} %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Contact', new_contact_path %>
