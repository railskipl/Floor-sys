<h1>Listing add_to_groups</h1>

<table>
  <tr>
    <th>Group</th>
    <th>Contact</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @add_to_groups.each do |add_to_group| %>
  <tr>
    <td><%= add_to_group.group_id %></td>
    <td><%= add_to_group.contact_id %></td>
    <td><%= link_to 'Show', add_to_group %></td>
    <td><%= link_to 'Edit', edit_add_to_group_path(add_to_group) %></td>
    <td><%= link_to 'Destroy', add_to_group, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Add to group', new_add_to_group_path %>
